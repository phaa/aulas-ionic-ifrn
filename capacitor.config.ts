import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aulas-ionic-ifrn',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
